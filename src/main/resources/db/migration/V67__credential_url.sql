-- 자격증명에 관리 페이지 주소를 붙인다.
--
-- 공유기 설정 페이지(192.168.0.1), 캐논 프린터 관리 콘솔처럼 "주소 + 로그인 정보"가 한 몸인
-- 것들을 다루기 위해서다. 지금까지는 id/pw만 볼트에 있고 주소는 각자 메모장·메신저에 있어,
-- 공유받은 사람이 "그래서 어디로 들어가지?"를 다시 물어야 했다.
--
-- 별도 '즐겨찾기' 도메인을 만들지 않은 이유: 필요한 조각이 이미 전부 자격증명에 있다.
-- 공유(credential_shares)·위키 연결(wiki_space_id)·비밀 암호화가 그대로 쓰인다.
-- 빠진 것은 주소 한 칸뿐이었다.
ALTER TABLE credentials ADD COLUMN url VARCHAR(500);
